# Plan

1. Package the existing plugin, align it with the pinned SDK and register the monorepo entry.
2. Keep history bounded and dismissible; preserve all-model color overrides and remove timeline polling writes.
3. Install independent loopback Codex Router and Jev user services on Dyaus, preserving existing models and private credentials.
4. Register Jev Routing in the native and BB model catalogs, then smoke test through Codex and BB.
5. Run focused tests, inherited runtime tests, live browser checks and root checks. Record any unrelated root failures.
6. Commit only this feature, push to the requested repository, and install the managed Git source on Dyaus.
