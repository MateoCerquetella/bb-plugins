Fresh-context acceptance QA — 2026-09-02

- AC-1/2/4: live Linux-server and Intel-Mac `bb touchbar snapshot --pretty`
  calls returned schemaVersion 1 and bounded thread cards. Acknowledged Naranja
  failures no longer appear as current red alerts; active cards remain.
- AC-3: `bb touchbar open thr_i47f5uua6r` ran on the Mac, returned exit 0, and
  reported that the exact thread opened. Unit tests cover unknown and ineligible
  IDs without mutation.
- AC-5: the native source no longer contains an `NSScrollView`; paged rows use
  direct native controls and the panel has initial item identifiers. The Intel
  Mac compiled and installed the new executable at
  `~/Applications/BBTouchBar.app`, and the running log identifies the
  `paged-ui 2026-09-02` build.
- AC-6: the README now explains native paging and acknowledged-error behavior,
  along with installation, privacy, limitations, and removal.
- AC-7: the focused Touch Bar suite passes 19/19, including phantom-error
  filtering and native interaction contracts. The complete root `npm run check`
  also passes.
- AC-8: `touchbar@0.1.0` is installed from the local package and reports
  running. Matrix-authorized package-consumer, clean-clone, integration,
  adapter-contract, end-to-end, and cross-platform checks pass.

The native panel was opened through `native/run.sh open` after installation.
The original user screenshot remains the failed pre-repair observation; this
note records the post-repair command and installation evidence without claiming
an automated physical-finger event.
