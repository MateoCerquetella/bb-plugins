# Plan

1. Extend mini-fleet parsing and metrics with bounded CPU/RAM guides and
   accessible passive threshold state; add boundary tests.
2. Add exact semantic nav-row discovery, reversible hidden-state tracking, and
   remount observation with cleanup tests.
3. Replace hard navigation with native hidden-row button activation and inline
   failure handling; forbid reload/Settings fallbacks in source tests.
4. Refine modal/page CSS to BB compact controls, tokens, spacing, typography,
   focus, selected state, and 390px containment.
5. Extend Chromium to prove zero visible page rows, threshold semantics, stable
   document identity across Open Host Monitor, dedicated page/editor, and
   screenshots.
6. Run typecheck, focused tests, build, live install/reload, mixed-host and
   persistence checks, workspace checks, diff audit, and isolated review; stop
   verified without merge or deploy.
