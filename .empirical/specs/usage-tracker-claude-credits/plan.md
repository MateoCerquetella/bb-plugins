# Plan

1. Render `formatCost(window.cost)` as noninteractive detail metadata only when
   normalized cost exists.
2. Style the credit line consistently with reset metadata.
3. Add renderer/source contracts and preserve existing normalization tests.
4. Update Usage Tracker README/changelog and bump manifest/lockfile to 0.1.9.
5. Run focused typecheck, tests, build, and whitespace verification.
6. Review and merge the issue branch, create the annotated tag and GitHub
   release, close #43, and verify marketplace semver resolution.
