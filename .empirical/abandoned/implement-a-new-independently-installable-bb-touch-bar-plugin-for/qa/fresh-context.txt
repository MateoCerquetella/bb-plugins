Fresh-context acceptance QA — 2026-08-31

- AC-1/2/4: live `bb touchbar snapshot --pretty` returned schemaVersion 1,
  bounded summary counts, three projected rows, normalized status/project fields,
  and no prompt/message/path/tool output.
- AC-3: direct and slot open targeted thr_ng93azwsxr; an unknown stop returned
  nonzero. Harness tests prove idle/unknown stops make no stop SDK call and an
  active stop targets exactly one id.
- AC-5: the preset parses as four global type-642 widgets with Always Show and
  two-second refresh; every tap action is open-card and none contains stop. The
  wrapper and installer parse as POSIX shell. The optional Swift source uses a
  direct Process executable plus argument array and contains no shell/private
  Touch Bar API.
- AC-6: plugin README documents Intel/BTT setup, All Apps behavior, privacy,
  limitations, the slot race, Control Strip boundary, removal, and sources.
- AC-7: 16 focused tests and the full root workspace check pass.
- AC-8: local install/reload reports touchbar@0.1.0 running. The 13-file packed
  consumer contains server metadata, companion, assets, license and docs, with
  no node_modules or source maps. An isolated source copy completed npm ci and
  the focused check successfully.

Physical BTT import/rendering remains target-hardware acceptance because this
execution host is Linux and has no enrolled Intel Touch Bar Mac.
