Fresh-context QA — Host Monitor machine-monitor adaptation
Date: 2026-09-03

PASS AC-1: monitor.ts and server.ts use the reference's bounded local CPU,
memory, root disk, load, directory, and Linux pressure collectors. Process
enumeration is now skipped entirely until attribution is enabled.

PASS AC-2: the app exposes 1h, 6h, 24h, 7d, and 30d ranges; storage history is
bucketed to at most 720 points and app chart rows insert explicit null gaps.

PASS AC-3: app.tsx/app.css contain four current metric cards, two SVG ECharts
panels, directory cards, memory diagnostics, container breakpoints at 760px,
460px, and 300px, and keyboard/touch overflow handling.

PASS AC-4: built metadata identifies pluginId host-monitor version 0.1.4 with BB
0.40.0 and SDK 0.4.21. The app registers id/path host-monitor and title Host
Monitor. Custom icon and logo assets are the only packaged images.

PASS AC-5: exact source and dist scans found no toast, Web Notification,
experimental_sidebarAccessory, sidebar-warning, threshold-alert, or role=alert
hook. Both failure states are inline role=status text.

PASS AC-6: source and dist scans found no fleet matrix, IP reveal, floating
monitor, host RPC, termination dialog, or process termination operation. The
package dry-run includes no host entry or obsolete fleet/process-control source.

PASS AC-7: server settings expose CPU/RAM/disk select thresholds with 90%
defaults and showProcessDetails=false.

PASS AC-8: npm run check passed across the complete workspace. Host Monitor's
focused suite passed 17 tests, SDK declarations, TypeScript, and production
build. npm pack --dry-run reported the intended 18 package entries.

No acceptance-criterion failures or unsupported checks found.

Repair verification after isolated review:

- History buckets are anchored to the requested range, use a +1ms ceiling, and
  are defensively sliced to MAX_RENDER_POINTS. A real in-memory SQLite test
  inserts 901 unaligned samples and proves the response stays at or below 720.
- Chart gaps use the range-derived expected interval and replace a row with a
  null sentinel instead of appending one, so chart rows cannot exceed samples.
- Directory and memory-diagnostic failures publish invalidations and appear in
  snapshot.collectorErrors, rendered as inline role=status text.
- Linux process RSS uses getconf PAGESIZE with a bounded fallback instead of a
  hard-coded 4 KiB page.
- Backend registration/snapshot, store bounding, chart-gap, and public-SDK-only
  tests raise the focused suite to 22 passing tests. The complete workspace
  check passes after these repairs.

Final live validation:

- The locally installed path plugin reports running status for all three
  services and its snapshot RPC returns live samples with no collector errors.
- Partial GNU du output is retained when unreadable nested /tmp entries cause a
  non-zero exit; a focused parser test distinguishes usable totals from a true
  failure. The focused suite now passes 23 tests.
- Fresh desktop and 390px screenshots show the dense reference-style panel,
  live charts and directory cards without the earlier routine /tmp status or
  any notification UI.
